# OpenOps

OpenOps is an evidence-driven operations investigation runtime.

The first milestone is intentionally narrow: investigate a Kubernetes Deployment whose Pods fail to become Ready because of a broken readiness probe.

See:

- `docs/product/charter.md` — current product definition
- `docs/vision.md` — long-term vision and motivation

# Phase 1 consumes no environment variables.
#
# Provider credentials and Kubernetes configuration are not implemented yet.
# Add only variable names with non-secret placeholders when code begins to consume them.
